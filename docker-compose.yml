version: '3.8'

services:
  mash-game:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mash-game
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - mash-network

networks:
  mash-network:
    driver: bridge
